// Eventuale codice JavaScript per migliorare l'interattività del sito
const backgroundImageUrl = 'leopardi.jpg'; // Assicurati di sostituire con il percorso corretto dell'immagine

  // Aggiungi l'immagine di sfondo al container
  const container = document.getElementById('container');
  container.style.backgroundImage = `url('${backgroundImageUrl}')`;
  container.style.backgroundSize = 'cover';
  container.style.backgroundPosition = 'center';
